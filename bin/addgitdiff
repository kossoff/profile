#!/bin/bash

VAL=`cat .gitconfig|grep diff_wrapper|awk -F "/" '{print $5;}'`

if [ "$VAL" == "diff_wrapper" ]; then
  exit 0
else
  echo "[diff]\n\texternal = $HOME/bin/diff_wrapper\n[pager]\n\tdiff =" >> ~/.gitconfig
fi
